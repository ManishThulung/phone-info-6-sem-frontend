import React from "react";

export interface PhoneData {
  key: React.Key;
  id: number;
  name: string;
  company: string;
  price: string;
}
export interface UserData {
  key: React.Key;
  id: number;
  name: string;
  email: string;
  role: string;
}
